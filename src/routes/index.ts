import { Router } from 'express';

// Import route modules here

// import otherRoutes from './other.routes'; // e.g., productRoutes, adminRoutes

const routes = Router();

// Register your route modules under appropriate paths

// router.use('/products', productRoutes); etc.

export default routes;
